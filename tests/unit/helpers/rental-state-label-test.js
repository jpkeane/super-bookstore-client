import { rentalStateLabel } from '../../../helpers/rental-state-label';
import { module, test } from 'qunit';

module('Unit | Helper | rental state label');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = rentalStateLabel(42);
  assert.ok(result);
});
